# Local development / Firebase Functions environment only.
# Never commit a real Gemini API key.
# The web client must NOT receive this value. The Functions endpoint provisions
# short-lived Live API tokens for the browser.
GEMINI_API_KEY=
