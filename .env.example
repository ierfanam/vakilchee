# Development only. Never commit a real API key.
# IMPORTANT: Vite embeds VITE_* values in the browser bundle.
# For commercial production, use a server-side proxy / ephemeral-token flow
# rather than shipping a long-lived Gemini API key to clients.
GEMINI_API_KEY=
